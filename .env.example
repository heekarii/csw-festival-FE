# Same base URL as existing table APIs (src/app/page.tsx).
NEXT_PUBLIC_API_BASE_URL=https://your-api.example.com

# Path after base, no leading slash. Default: waiting (see docs/API_WAITING.md)
# NEXT_PUBLIC_WAITING_PATH=waiting