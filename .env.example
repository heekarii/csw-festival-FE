# Same base URL as existing table APIs (src/app/page.tsx).
NEXT_PUBLIC_API_BASE_URL=https://your-api.example.com

# POST waiting registration. Default path: waiting
# NEXT_PUBLIC_WAITING_PATH=waiting

# POST queue position lookup (phone -> queueNumber). Default: waiting/position
# NEXT_PUBLIC_WAITING_POSITION_PATH=waiting/position