# API origin without trailing slash (required when calling a real backend)
VITE_API_BASE_URL=https://your-api.example.com

# Optional override for POST path (default: /api/reservations)
# VITE_RESERVATION_PATH=/api/reservations