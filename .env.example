# Same base URL as existing table APIs (src/app/page.tsx).
NEXT_PUBLIC_API_BASE_URL=https://your-api.example.com

# Path after base, no leading slash. Default matches reservation-page Vite default.
# NEXT_PUBLIC_WAITING_PATH=api/reservations